server:
  port: 8080
  servlet:
    context-path: /api

app:
  python-service:
    url: "http://python-service:8000"
    timeout: 5000
    retry:
      max-attempts: 3
      backoff: 1000

logging:
  level:
    com.persiantts: DEBUG
    org.springframework.web: INFO
